#include <stdio.h>
/**
 * main - check the code
 * @argc: arg int
 * @argv: arg is a pointer char
 * Return: Always 0 (sucess)
 */

int main(int argc __attribute__((unused)), char *argv[])
{
		printf("%s\n", argv[0]);
			return (0);
}
